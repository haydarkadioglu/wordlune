
"use client";

// This component is not actively used as the login process is currently bypassed.
// The application defaults to a mock user. See src/components/auth/AuthProvider.tsx.
export default function LoginForm() {
  return null;
}
